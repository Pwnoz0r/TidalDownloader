namespace TidalDownloaderUI.Enums
{
    public enum ConfigSections
    {
        LoginInfo,
        ThemeSettings,
        LibSettings
    }

    public enum ConfigLoginKeys
    {
        TidalUserName,
        TidalPassword
    }

    public enum ConfigThemeKeys
    {
        ThemeColor,
        ThemeStyle
    }

    public enum ConfigLibKeys
    {
        LibDirectory
    }
}
